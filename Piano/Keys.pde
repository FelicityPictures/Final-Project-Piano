class Keys{
  int w,l,x,y,c;
  char note;
 Keys(boolean col,int x,int y,char note){
  int keyWidth = 50;
  int keyLength = 200;
   if(black){
     fill(0);
     keyWidth = 30;
     keyLength = 100;
   }
   rect(x,y,keyWidth,keyLength);
 }
 
 
}
